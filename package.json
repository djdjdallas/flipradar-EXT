{
  "name": "flipchecker-extension",
  "version": "1.3.0",
  "private": true,
  "description": "FlipChecker Chrome Extension - Compare Facebook Marketplace prices to eBay sold listings",
  "scripts": {
    "build": "node esbuild.config.js",
    "watch": "node esbuild.config.js --watch",
    "clean": "rm -rf dist",
    "package": "node scripts/package.js"
  },
  "devDependencies": {
    "archiver": "^7.0.0",
    "esbuild": "^0.20.0"
  }
}
